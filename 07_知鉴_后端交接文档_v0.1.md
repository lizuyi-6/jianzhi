# 知鉴后端交接文档 v0.1

## 1. 交接结论

后端核心链路已完成，前端可以从 HTTP API 开始接入。当前实现不包含前端。

已验证：

- 184 个 canonical SourceDocument
- 7 个经过 Evidence 校验的 ExperienceRecord
- DiscussionLens、Comparison、A/B/C ReadingSet
- closed-world RenderPacket
- 17 个自动化测试全部通过
- TypeScript typecheck、数据校验、生产构建全部通过
- npm audit：0 vulnerabilities
- 编译后的 dist/server.js 已通过 health、ready、meta、RenderPacket 冒烟

## 2. 目录与职责

- backend/src/core/contracts.ts：Zod 数据契约和错误码。
- backend/src/core/evidence.ts：quote/start/end/source 精确证据校验。
- backend/src/core/dataset.ts：JSONL 来源加载和重复 ID 校验。
- backend/src/core/experience-store.ts：ExperienceRecord、来源和 Evidence 完整性。
- backend/src/core/contrast.ts：DiscussionLens 维度发现，支持同条件不同选择。
- backend/src/core/compare.ts：Same / Different / Unknown。
- backend/src/core/retrieve.ts：独立 A/B/C 角色选择，B 可以为空。
- backend/src/core/render.ts：组装 RenderPacket，不生成未经 Evidence 支持的事实。
- backend/src/adapters/zhihu-search.ts：知乎官方搜索 API 适配器。
- backend/src/services：来源、Lens、ReadingSet 和 RenderPacket 服务。
- backend/src/app.ts：路由、请求校验、错误 envelope、请求 ID 和日志。
- backend/src/server.ts：配置启动和优雅关闭。
- backend/src/openapi.ts：OpenAPI 契约。
- backend/data/source-documents.jsonl：canonical 来源。
- backend/data/experience-records.jsonl：人工确认的 ExperienceRecord。
- backend/data/README.md：数据 provenance 和选择规则。

## 3. 本地启动

远端工作区根目录为 /home/break/jianzhi。

~~~bash
cd backend
npm install
cp .env.example ../.env.local
# 在 ../.env.local 写入 ZHIHU_ACCESS_SECRET
npm run check
npm run dev
~~~

默认地址：http://localhost:3001。

生产构建和启动：

~~~bash
cd backend
npm run build
ZHIHU_ACCESS_SECRET='...' PORT=3001 npm run start:prod
~~~

生产环境通过环境变量注入 Secret，不要把 .env.local 放进镜像或提交仓库。

## 4. 前端接入顺序

1. GET /health：进程存活。
2. GET /ready：canonical 数据和 ExperienceRecord 已可用。
3. GET /api/v1/meta：读取能力、来源数、Experience 数。
4. GET /api/v1/sources：来源列表和分页。
5. POST /api/v1/lens：展示可靠维度和 observed values。
6. POST /api/v1/render-packet：主页面优先消费此接口。
7. GET /api/v1/sources/:sourceId：点击来源时展示原文元数据。

Golden 请求：

~~~http
POST /api/v1/render-packet
Content-Type: application/json

{
  "question_key": "golden-read-work",
  "values": {
    "family_pressure": "FAMILY_PRESSURE"
  }
}
~~~

该请求稳定返回 COMPARABLE、COUNTER_EXPERIENCE、CLASSIC 三个 cards，warnings 为空。

## 5. RenderPacket 约束

每个 card 包含：

- role：COMPARABLE、COUNTER_EXPERIENCE 或 CLASSIC。
- source_id、title、url：来源身份。
- same_dimensions、different_dimensions、unknown_dimensions：相对用户条件的比较结果。
- why_read_codes：前端本地化的结构化理由码。
- evidence[]：原始 quote、start、end、field、source_id。

前端不得：

- 把 A/B/C 重新排序成总榜。
- 把 Unknown 渲染成推测。
- 删除 source URL 或 Evidence quote。
- 根据作者名、标题或常识补全背景。
- 在 B 为空时伪造反向经验。
- 把多个来源压缩成一个统一建议。

## 6. 错误契约

所有错误统一为：

~~~json
{
  "error": {
    "code": "INVALID_REQUEST",
    "message": "Request validation failed",
    "request_id": "req-123",
    "retryable": false
  }
}
~~~

主要错误：

- INVALID_REQUEST：请求体或 query 不合法。
- NO_USABLE_EXPERIENCE：没有通过校验的经验记录。
- NO_RELIABLE_DIMENSION：没有足够可靠的 Lens 维度。
- SOURCE_NOT_FOUND：canonical 来源不存在。
- RATE_LIMITED：上游限流，可退避重试。
- UPSTREAM_FAILED：知乎官方 API 失败，可有限重试。
- NOT_READY：服务尚未达到数据就绪状态。

## 7. 数据与真实性边界

当前来源是知乎官方搜索 API 返回的 Answer/Article 搜索片段，不宣称覆盖某个 Question 的全部回答。question_id 可能为空。前端必须保留 source_mode=official_api_search provenance。

ExperienceRecord 只有在原文明确表达经历、条件和选择时才建立。无法确认的背景信息进入 Unknown 或不进入记录。

Access Secret 已用于采集，不应继续长期使用；交接后应立即旋转，并将新 Secret 只注入部署环境。

## 8. 测试与发布门禁

发布前在 backend 目录执行：

~~~bash
npm run check
~~~

该命令依次执行 typecheck、canonical 数据校验、17 个测试和生产 TypeScript 构建。

额外检查：

- npm audit --audit-level=low
- GET /health
- GET /ready
- GET /api/v1/openapi.json
- 使用 Golden 请求验证 A/B/C 和 Evidence 数量

## 9. 后续工作

1. 旋转已经使用过的 Zhihu Secret。
2. 生产部署设置严格的 CORS_ORIGIN，不要使用 *。
3. 将官方 API 配额、缓存目录和结构化日志接入部署平台。
4. 持续追加人工标注的 E2/E3 数据；不合格导入会 fail closed。
5. 前端只展示 RenderPacket，不复制后端比较和检索逻辑。

## 10. 当前验证快照

- source count: 184
- experience record count: 7
- tests: 17 passed
- npm audit: 0 vulnerabilities
- production smoke: health/ready/meta/render-packet passed
- running backend process: none after smoke cleanup
