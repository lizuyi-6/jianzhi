# 知鉴 / ZhiLens 执行手册 v0.1

> 用途：作为接下来开发工作的唯一执行入口，防止在数据、产品边界和演示目标未满足时漂移到 UI 堆功能。
> 关联规划：`05_知鉴_开发任务规划_v0.1.md`
> 更新时间：2026-08-17
> 当前阶段：M0 数据 Gate 探查，尚未进入正式 UI 开发。

## 0. 工作规则

### 0.1 唯一目标

交付一个可公网访问的 Golden Demo，在 30 秒内让评委理解：知鉴恢复真人经验的条件与分歧，不替用户做最终判断。

### 0.2 执行顺序

严格遵守：

`数据 Gate -> 合法快照 -> Schema -> Evidence Validator -> Contrast -> A/B/C 检索 -> API -> UI -> 条件变化 -> 评测 -> 部署 -> 演示`

不得反向开发。没有稳定的 ReadingSet JSON，不开始页面视觉细化；没有合法正文，不允许让模型“根据摘要补全文本”。

### 0.3 每个任务的完成格式

每完成一个任务，必须留下四项记录：

1. 做了什么：文件、命令、接口或决策。
2. 观察到什么：实际输出、数量、错误码、样本路径。
3. 是否通过：对照明确验收条件，不使用“基本可以”。
4. 下一步是什么：只允许指向本手册中的任务 ID。

任务不能以“顺便优化”“先做个大概”结束。发现新需求必须进入变更记录，不能直接插入当前任务。

### 0.4 停止与升级规则

遇到以下任一情况，立即停止当前链路并记录，不用模型或临时代码掩盖：

- 数据只有摘要，无法绑定可靠原文 Span。
- 接口返回结果不能证明属于当前 Question 或合法来源范围。
- 任一可见事实没有 source_id + evidence_span。
- 需要新增长期画像、统一匹配分数、最终推荐、胜率或成功率才能继续。
- 需要未授权爬虫、绕过限流、伪造实时结果或把 Fixture 伪装成线上数据。
- API、模型或部署环境不确定，却开始依赖其具体行为写核心逻辑。

## 1. 当前事实与风险结论

### 1.1 已确认

- 工作空间：`/home/break/jianzhi`。
- 启动包为 5 份规格文档和 README，目前没有源码仓库。
- Node.js 22、npm 10、Python 3.14、Git 已安装。
- 知乎 Bearer Token 鉴权探针成功：HTTP 200、平台 Code 0。
- Secret 仅保存在 `.env.local`，权限 600，未写入本手册。
- API 域名为 `https://developer.zhihu.com`，请求需要 `Authorization` 和秒级 `X-Request-Timestamp`。

### 1.2 低配额探针观察

探针查询了 5 个职业选择类关键词，每次 Count=10：

- 5/5 请求 HTTP 200，Code=0。
- 每次返回 10 条，类型为 Answer/Article。
- 所有响应 `HasMore=false`。
- `ContentText` 长度范围为 53-1,219 字符，存在明显短文本。
- 返回字段包含 ContentID、ContentText、Url、作者、投票、评论、AuthorityLevel、RankingScore 等。
- 搜索结果没有直接证明是某一个 Question 的完整 Answers 集合。

探针样本在：`.analysis/data-gate/auth_probe.json` 和 `.analysis/data-gate/probes/`。

### 1.3 当前结论

当前仅通过“鉴权可用”和“搜索返回结构化结果”两项，未通过“Evidence-backed extraction 可用”这一核心 Gate。

原规格要求 5 类 Query x 前 20 条，但当前搜索接口文档称单次最多 10 条且 HasMore 固定 false。不得把 5 x 10 结果冒充 5 x 20；需改为以下二选一并记录决策：

- 方案 A：10 个 Query x 10 条，保持 100 条样本总量，但样本覆盖更多搜索表述。
- 方案 B：5 个 Query 各做多个合法语义变体，去重后达到 100 条，并记录每条召回 Query。

如果仍无法获得完整正文或可靠原文定位，必须切 SearchAdapter + 合法快照，或重新做产品 Pivot；不能继续假设 QuestionAdapter 存在。

## 2. 任务总表

状态只允许使用：`TODO`、`DOING`、`BLOCKED`、`DONE`。同一时间只能有一个 `DOING` 任务。

| ID | 优先级 | 任务 | 依赖 | 通过条件 |
|---|---|---|---|---|
| M0-01 | P0 | 冻结 API 入口、授权和限额 | 已有探针 | 文档、请求头、错误码和频率规则有记录 |
| M0-02 | P0 | 完成 100 条合法样本 Gate | M0-01 | 每条有来源、类型、长度和 E0-E3 标注 |
| M0-03 | P0 | 判断正文与 Evidence 可行性 | M0-02 | E2/E3 达标，且可稳定定位原文 Span |
| M0-04 | P0 | 冻结 Golden Question 与 snapshot | M0-03 | 8-12 条强样本可离线重放 |
| E-01 | P0 | 初始化 Git/npm/TypeScript 工程 | M0-04 | 干净目录可安装、测试、启动 |
| E-02 | P0 | 编写 Zod schema 与错误码 | E-01 | schema_version、序列化、非法输入测试通过 |
| A-01 | P0 | 实现 Adapter 与 SourceDocument | E-02 | Fixture/官方入口可替换，字段不混用 |
| A-02 | P0 | 实现 Extraction 与 Evidence Validator | A-01/E-02 | 无 Span 的事实变 Unknown/丢弃 |
| A-03 | P0 | 实现 Contrast Discovery | A-02 | 2-4 个维度，来自当前语料，不来自常识 |
| A-04 | P0 | 实现确定性 Comparison 与 A/B/C | A-03 | A/B/C 不共用 Top 3 总分，B 可空 |
| B-01 | P0 | API orchestration 与四级缓存 | A-04 | 前端不直连 LLM，缓存键可追溯 |
| F-01 | P0 | Question mock 与入口 | B-01 | 未点击时默认阅读无变化 |
| F-02 | P0 | Lens Panel 与当前条件 | F-01/B-01 | 只询问 Lens 中维度，允许 Unknown |
| F-03 | P0 | Reading Set 与 Evidence | F-02/A-04 | Same/Diff/Unknown 和原文回溯完整 |
| F-04 | P0 | 条件修改闭环 | F-03 | 修改一个条件至少一席变化且可解释 |
| Q-01 | P0 | Gold Set、指标和回归 | A-02/A-04/F-04 | Evidence Precision >=95%，Explanation=100% |
| R-01 | P0 | Fail-closed、部署和 snapshot fallback | Q-01 | 断网/限流仍可诚实回放 |
| R-02 | P0 | 演示和提交材料 | R-01 | 3 次演示均 <3 分钟，密钥/文案/合规检查过 |
| P1-01 | P1 | 评论、第二问题、Debug、埋点 | R-01 | 仅在主链稳定后安排 |

## 3. 当前阶段执行卡：M0 数据 Gate

### M0-01：冻结 API 事实

**动作**

- 保存官方接口文档、请求样例和错误码。
- 明确 Token 只能从环境变量读取，禁止进入源码、fixture、日志和截图。
- 写一个可重复的 probe 脚本，默认单次 Count 不超过 10，带超时和请求间隔。
- 记录接口是否有正文、详情读取、Question -> Answers、分页和评论能力。

**验收**

- 同一命令可在无输出 Secret 的情况下复现 HTTP 状态和摘要统计。
- 任何失败可归类为鉴权、限流、参数、无结果或正文不足。

### M0-02：100 条样本采集与人工标注

**建议采样**

使用 10 个 Query x 10 条，覆盖：读研/工作、第一份 Offer、大厂/创业、转 AI、工程/研究等；每条保存：query、获取时间、source_mode、ContentID、ContentType、Title、Url、文本原样、平台信号。

**E0-E3 标注建议**

- E0：只有标题或极短摘要，不能判断经历。
- E1：有观点，但无明确亲历/条件/选择。
- E2：有明确经历、至少一个决策前条件和选择，可定位文本片段。
- E3：有经历、多个决策前条件、选择、结果/反思，且证据片段清晰。

**验收阈值**

- 强通过：E2/E3 >= 30%，且目标问题至少 5 条强可比较经验。
- 条件通过：10%-30%，但官方支持完整正文 follow-up。
- No-Go：短摘要、无完整读取、E2/E3 低于 10%。

注意：同一内容被多个 Query 召回只算一条 source，不得用重复召回虚增覆盖率。

### M0-03：Evidence 可行性核查

逐条回答：

1. 能否从返回文本截取稳定 quote？
2. Span 是否能在用户展开时对应到公开原文？
3. 返回的 ContentID/Url 是否足以回到唯一来源？
4. 内容是否是摘要，且可能由平台重写或截断？
5. Answer 与 Article 是否需要不同的解析策略？
6. 是否存在删除、登录、地区或权限导致的不可回溯？

任何一项不能确认，字段必须标 Unknown；不能使用模型补全文本。

### M0-04：冻结 snapshot

snapshot 必须包含原始响应、请求 Query、获取时间、来源模式、schema_version 和人工标注；禁止把 Secret、Cookie、个人隐私或未授权内容纳入版本库。

Golden Question 只在 M0-03 通过后冻结。若只拿到 SearchAdapter 摘要，Demo 文案必须明确“基于官方搜索返回的公开片段”，不能声称展示完整 Question Answers。

## 4. 实现阶段执行卡

### E/A：数据管线

固定中间产物：

`SourceDocument -> ContentQualification -> ExperienceRecord -> DiscussionLens -> CurrentDecisionContext -> ComparisonResult -> ReadingSet -> RenderPacket`

规则：

- LLM 只做分类、抽取和 Question-local 归一化。
- 普通程序负责 Evidence validator、对比、席位选择和错误状态。
- Contrast 模块不读 raw 全文，只读 ExperienceRecord。
- Renderer 只消费 RenderPacket，服务端做 closed-world 校验。
- Outcome、Reflection 不能倒灌成决策前条件。

### B：API 与缓存

缓存键必须包含 source hash、schema 版本、prompt/model 版本和上下文 hash；至少分为 source、extract、lens、reading 四层。

失败处理：

- 401/403：停止重试，记录鉴权错误。
- 429/30001：有限退避，优先读取缓存，不并发打爆接口。
- 5xx/超时：有限重试后进入 snapshot fallback。
- 返回空或短文本：INSUFFICIENT_TEXT，不进入条件发现。

### F：前端主链

页面状态必须显式建模：Idle、Analyzing、Ready、Insufficient、NoComparable、NoCounter、RateLimited、Error。

用户界面只允许出现：Same、Different、Unknown、为什么这么说、相似处境/另一种选择、原文依据。禁止出现：推荐你选择、最适合你、匹配度、成功率、决定因素、作者可能是/应该是。

### Q/R：验收与交付

每次改动至少执行对应层测试：

- schema/validator：单元测试和非法 fixture。
- contrast/comparison/retrieval：固定 fixture 快照测试。
- API：缓存命中、限流、空文本、RenderPacket 非法测试。
- UI：Golden Flow、Evidence 展开、条件修改、空席状态。
- 提交前：无缓存、缓存、断网/限流各一次；Golden Demo 连跑三次。

## 5. 已知坑与预防措施

| 坑 | 早期信号 | 预防/止损 |
|---|---|---|
| 把搜索摘要当完整正文 | 文本很短、Evidence 无法回原文 | 进入 INSUFFICIENT_TEXT；先验证详情能力 |
| 把搜索结果当同题 Answers | 没有 question_id 或同题归属 | SourceDocument 标记 SearchAdapter，不声称完整集合 |
| 5x20 与接口上限冲突 | Count=20 仍只返回 10、HasMore=false | 改为 10x10 或合法 Query 变体，并记录采样方式 |
| 重复召回虚增样本 | 同一 ContentID 多次出现 | 按 ContentID 去重，保留召回 Query 列表 |
| 模型补全作者背景 | 没有 quote 但出现学历/动机 | Evidence validator 丢弃；Unknown 一等状态 |
| Outcome Leakage | 用结果解释决策时条件 | 分离 pre-decision/outcome/reflection 字段和测试 |
| 三席退化为统一排序 | 页面出现总分/匹配度 | A/B/C 独立选择器和禁词扫描 |
| B 席被强行填满 | 找不到反向经验仍显示卡片 | 合法空席 NO_COUNTER_EXPERIENCE |
| UI 先行导致契约漂移 | 前端开始读取 LLM 自由文本 | 只允许 RenderPacket/typed API |
| 实时 API 让演示不稳定 | 限流、超时、结果变化 | Golden snapshot fallback，并如实标注缓存 |
| 缓存污染新逻辑 | prompt/schema 改了仍命中旧结果 | cache key 必含版本和 source hash |
| Secret 泄露 | 出现在 git diff、日志、截图 | 只读 env，`.gitignore`，提交前扫描，必要时轮换 |
| 依赖漂移 | 36h 后升级包/换模型 | 36h Freeze Line，锁版本，只修 P0 |
| 把 P1 当主线 | 先做评论、第二问题、Debug | P1 只有 M3 通过后才解锁 |

## 6. 防漂移看板模板

每次工作开始前复制以下块并更新：

```
日期/轮次：
当前任务 ID：
本轮唯一产出：
依赖是否满足：是/否
计划修改文件：
验收命令或手工步骤：
停止条件：
结果：TODO/DOING/BLOCKED/DONE
证据路径：
下一任务 ID：
```

### 变更记录

| 日期 | 提议 | 触碰的不变量 | 决策 | 影响文档/任务 |
|---|---|---|---|---|
| 2026-08-17 | 将 5x20 调整为 10x10 或 Query 变体 | 数据 Gate 可复核性 | 待 M0-02 冻结 | M0-02、M0-03、PRD 数据入口 |

### 每日收口

- [ ] 今日完成任务均有证据路径。
- [ ] 没有把 BLOCKED 任务标成 DONE。
- [ ] 未新增未评审 P1 功能。
- [ ] API/模型/缓存版本有记录。
- [ ] Secret、Cookie、个人信息未进入提交内容。
- [ ] 下一轮只有一个明确 DOING 任务。

## 7. 现在应该执行什么

下一轮只执行 M0-02，不开发 UI：

1. 决定 10x10 或 Query 变体采样方案。
2. 写入可重复的采样脚本和人工标注模板。
3. 按 ContentID 去重，采集 100 条合法样本。
4. 标注 E0-E3、Experience、Decision、pre-decision Context 和 Evidence Span。
5. 输出 M0-03 结论：通过、条件通过或 No-Go。
6. 只有通过或明确选择合法降级后，才创建正式源码工程。

## 8. 需要维护的文件

- `05_知鉴_开发任务规划_v0.1.md`：阶段规划和范围基线。
- `06_知鉴_执行手册_v0.1.md`：当前文件，日常执行入口。
- `.analysis/data-gate/`：探针、样本统计和 Gate 证据，不放 Secret。
- `.env.local`：本地 Secret，不提交、不复制到日志。

## 9. 交付前检查

- [ ] 公网 Demo 匿名可访问或登录步骤明确。
- [ ] README 能在干净环境启动。
- [ ] Golden snapshot 可离线回放。
- [ ] Evidence 展开不会失败。
- [ ] Same/Different/Unknown 逻辑有测试。
- [ ] A/B/C 不是统一 Top 3。
- [ ] B 席允许为空。
- [ ] 无最终建议、匹配度、胜率和敏感画像。
- [ ] 无未授权抓取、未授权素材、Secret、Cookie、测试个人信息。
- [ ] 最终版本冻结后只修 P0 阻断问题。
