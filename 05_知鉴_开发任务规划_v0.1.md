# 知鉴 / ZhiLens 开发任务规划 v0.1

> 基于 Product Anchor、PRD、Constraints & Guardrails、AI & Data Contract、48h Build Playbook 整理。
> 当前状态：仅有产品与工程规格文档，无源码、仓库、依赖和部署配置。

## 1. 项目理解

知鉴是知乎复杂多回答问题中的 AI 原生阅读工具。它从真人回答中恢复“经验、条件、选择、分歧、来源”结构，帮助用户得到一组最少但互补的三席 Reading Set，而不是生成一个综合建议。

Golden Flow：Question mock -> 按我的情况看 -> 2-4 个有证据的分歧条件 -> 用户确认当前条件 -> A/B/C 三席阅读集合 -> 展开原文 Evidence -> 修改一个条件后集合发生可解释变化。

三席职责：

- A：和用户公开条件可比较的亲历。
- B：相似处境下的另一种选择，找不到时允许空席。
- C：公共质量坐标下值得阅读的经典回答，不要求与用户相似。

绝对边界：不输出最终人生建议，不显示匹配度/胜率，不构造长期画像，不补全作者背景，不把三席做成统一评分 Top 3，不用未经授权的批量爬虫。

## 2. 当前判断与工程基线

### 2.1 当前缺口

- 官方赛事 API / Skill 的正文能力、Answer 定位、分页、限流和评论能力未实测。
- Golden Question、合法数据 snapshot 和人工 Gold Set 尚未冻结。
- 模型供应商、结构化输出能力、额度和超时策略未确定。
- 没有源码、Git 仓库、CI、部署目标或环境变量规范。
- 团队人数未知；本计划默认单人顺序开发，并给出可并行合流点。

### 2.2 建议技术栈

为适配 48h 和当前 Node.js 22 + npm 10 环境，建议采用全 TypeScript：

- Web：React + Vite + TypeScript。
- API：Fastify + TypeScript。
- Contract：Zod，所有 schema 携带 schema_version。
- Cache：开发期文件缓存或 SQLite；不引入向量数据库/图数据库。
- Test：Vitest（规则与契约）+ Playwright（Golden Flow）。
- Repo：npm workspaces，web/api/core 三包或轻量单仓分层。
- AI：通过 ModelAdapter 隔离具体供应商；严格 JSON schema 输出。
- Deploy：M0 冻结目标；要求一个公网 URL、服务端密钥、Golden snapshot 离线降级。

选择理由：前后端共享类型，减少契约漂移；现有环境无需安装另一套包管理器；单仓更适合 48h 快速迭代。

## 3. Gate 与里程碑

| Gate | 截止 | 交付物 | 通过标准 | 失败动作 |
|---|---:|---|---|---|
| M0 数据 Gate | +2h | API 实测报告、Adapter 决策、Golden Question 候选 | E2/E3 >= 30%，每类至少 5 条强可比较经验；或 10%-30% 且有官方全文 follow-up | 切 SearchAdapter；仍不满足则 Pivot，并先更新上层文档 |
| M1 Vertical Slice | +12h | 8-12 条合法样本跑出 ReadingSet JSON | Source -> Extraction -> Lens -> Compare -> A/B/C 全链；所有可见事实有 Evidence | 停 UI，修 schema、validator、prompt |
| M2 End-to-End | +24h | 可点击本地 Demo | Question -> Lens -> Context -> Reading Set -> Evidence 完整可走 | 砍全部 P1，只保主链 |
| M3 Golden Demo | +36h | 稳定 Golden Flow | 修改“工程开发 -> 算法/研究”后至少一席变化且解释成立 | 冻结主功能，只修阻断问题 |
| M4 Submission Ready | +44h | 公网 Demo、缓存、异常态、README | 冷/热缓存与限流降级均可演示 | 不改 schema、不换模型 |
| M5 Final | +48h | 视频、计划书、最终版本 | Golden Demo 连跑 3 次均 < 3 分钟 | 只修提交阻断 Bug |

## 4. 任务 Backlog

### P0-0：数据 Gate 与关键决策（0-2h）

- D-001 获取官方 API / Hackathon Skill 文档、凭据、额度和调用样例。
- D-002 执行 5 类 Query x 前 20 条，记录正文长度、ID、URL、分页、评论和全文 follow-up。
- D-003 人工标记 E0-E3，输出可用率与 Gate 结论。
- D-004 冻结 QuestionAdapter / SearchAdapter / Pivot 决策。
- D-005 冻结 1 个 Golden Question 和 8-12 条合法 snapshot，明确 source_mode。
- D-006 冻结模型供应商、部署目标和环境变量清单。

验收：形成可复核的数据 Gate 报告；Gate 未通过时不得进入伪造正文的实现路径。

### P0-1：仓库与强类型契约（2-4h）

- E-001 初始化 Git、npm workspace、TypeScript、Lint、Vitest 和 env example。
- E-002 建立 web/api/core 或等价目录边界。
- E-003 实现 SourceDocument、ContentQualification、ExperienceRecord、DiscussionLens、CurrentDecisionContext、ComparisonResult、ReadingSet、RenderPacket。
- E-004 建立统一错误码、schema_version 和序列化测试。
- E-005 提交 Golden fixtures，并记录合法来源与 snapshot 时间。

验收：前后端只消费 typed contract；非法结构在服务端被拒绝。

### P0-2：Extraction 与 Evidence Validator（4-8h）

- A-001 实现 FixtureAdapter；为已冻结官方入口保留同一 Adapter 接口。
- A-002 实现 Qualification + Extraction 的模型调用和结构化输出。
- A-003 校验 quote 与 start/end 在原文中精确匹配；失败字段丢弃为 Unknown。
- A-004 严格分离 pre-decision context、decision、outcome、reflection，防止 Outcome Leakage。
- A-005 增加 prompt/schema/model 版本缓存键和阶段日志。

验收：所有进入后续链路的 Context/Decision/Outcome 均绑定有效 source_id 和原文 Span。

### P0-3：Contrast、Comparison 与三席检索（8-12h）

- A-006 实现维度标准化、variation、coverage、decision contrast、user answerable 和 dedup 规则。
- A-007 实现 pairwise support、反例搜索和 2-4 个 DiscussionLens 维度选择。
- A-008 实现确定性的 Same/Different/Unknown；Unknown 不参与抵消。
- A-009 分别实现 Slot A、B、C 选择器，不共享统一总分。
- A-010 生成首份端到端 ReadingSet JSON 和选择原因日志。

验收：8-12 条 fixture 可稳定生成 1-3 个维度及合法三席；B 席缺失时返回 NO_COUNTER_EXPERIENCE。

### P0-4：API 与产品主链（12-20h）

- B-001 实现 question、lens、context/reading-set、source/evidence API。
- B-002 实现 source/extract/lens/reading 四级缓存与失效策略。
- F-001 实现 Question mock 和“按我的情况看”入口，默认阅读不受影响。
- F-002 实现 Lens 状态机：Idle、Analyzing、Ready、Insufficient。
- F-003 实现 2-4 个分歧条件、依据展开和“不确定/都不是/不重要”。
- F-004 实现 CurrentDecisionContext 提交。
- F-005 实现 A/B/C 卡、Same/Different/Unknown 和空席状态。
- F-006 实现原文为主体的 Evidence 展开/定位。

验收：本地主链完整可点击；冷分析超过 0.8s 显示阶段进度；前端不直连模型。

### P0-5：条件变化闭环与 Fail-closed（20-30h）

- B-003 修改任一条件后按 context hash 重算 Reading Set。
- B-004 产生仅基于结构差异的“为什么变了”，不生成最终建议。
- B-005 实现 INSUFFICIENT_TEXT、NO_USABLE_EXPERIENCE、NO_RELIABLE_DIMENSION、NO_COMPARABLE_EXPERIENCE、NO_COUNTER_EXPERIENCE、USER_CONDITION_UNCOVERED、RATE_LIMITED、RENDER_PACKET_INVALID。
- B-006 Renderer 仅接收 RenderPacket，并做服务端 closed-world 校验。
- Q-001 增加每阶段耗时、候选数、有效 Experience 数、维度数、席位原因和 fallback_code 日志。

验收：Golden 条件变化至少替换一个席位；所有失败都显示明确降级态，不展示半结构化数据。

### P0-6：评测、回归与可信度（30-36h）

- Q-002 建立 10-20 条人工 Gold：知识类型、Context Span、Decision、Contrast 支持 Pair。
- Q-003 计算 Evidence Precision、Hallucinated Fact Rate、Contrast Precision@3、Explanation Rate。
- Q-004 添加证据 validator、comparison、retrieval 和错误码单元测试。
- Q-005 添加 Golden Flow Playwright 测试和禁用文案扫描。
- Q-006 执行 baseline 对比，记录大 Prompt 与结构化链路差异。

验收：Evidence Precision >= 95%，Explanation Rate = 100%，Contrast Precision@3 >= 0.8，幻觉事实接近 0。

### P0-7：部署、演示与提交（36-48h）

- R-001 冻结主功能，打磨加载、卡片层级、Evidence 展开和变化反馈。
- R-002 部署公网并配置服务端密钥、健康检查和 Golden snapshot fallback。
- R-003 验证无缓存、有缓存、限流/断网三种启动路径。
- R-004 完成 README、架构图、运行方式、env example、数据来源与赛事合规说明。
- R-005 录制 90-180 秒演示视频，准备 3 分钟路演脚本。
- R-006 Golden Demo 连跑 3 次；扫描密钥、个人信息和违规文案。

验收：匿名可访问公网链接；三次演示均 < 3 分钟；断网/限流时仍可诚实回放已缓存 snapshot。

### P1：仅在 M3 提前通过后执行

- P1-001 精选评论作为反例或时间修正候选。
- P1-002 增加 1-2 个备用 Question，复用完全相同 Pipeline。
- P1-003 开发态 Debug 面板。
- P1-004 基础产品埋点与漏斗。

## 5. 依赖与执行顺序

硬依赖链：D-001 -> D-005 -> E-003 -> A-002 -> A-003 -> A-006 -> A-008/A-009 -> B-001 -> F-002~F-006 -> B-003 -> Q-003 -> R-002。

不得并行绕过的关卡：数据 Gate、schema、Evidence validator、Golden snapshot。UI 可以在首份稳定 ReadingSet JSON 出现后并行，但只能消费冻结 contract。

多人并行建议：

- Data/AI：D-*、A-*、Q-002/Q-003。
- Backend：E-*、B-*、缓存、错误码。
- Frontend/Product：F-*，只使用 typed API/fixture。
- Demo/QA：Q-*、R-*；36h 后拥有最高优先级。

## 6. Definition of Done

- 用户 30 秒内能理解三席不是 Top 3。
- 每条用户可见判断可回到准确原文 Span。
- Unknown、空席和弃权均是一等状态。
- 修改一个关键条件后 Reading Set 真实变化，并能用结构差异解释。
- 全流程不出现“你应该”、匹配度、胜率、决定因素或敏感画像。
- Golden Demo 不依赖实时 API 稳定性，且明确标注 snapshot/cache。
- API 失败不会暴露脏 JSON、密钥或未经验证事实。
- 项目可从 README 在干净环境启动，自动测试可复现。

## 7. 开工前必须确认

1. 官方 Hackathon Skill / API 的访问方式、凭据和授权范围。
2. 团队人数与 48h 窗口是否仍为实际约束。
3. 可用模型供应商、结构化输出能力、额度与密钥。
4. 公网部署平台、域名/账号权限和是否需要登录。
5. Golden Question 是否确定为“有不错后端 Offer，要不要读研”，以及样本内容的合法取得方式。

在第 1 项未确认前，下一步应只做数据 Gate 脚本与评测模板，不应投入正式 UI。
